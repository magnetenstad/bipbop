
greet :: (name) -> {
  >_ ('Hello ' + name + '!');
};

sum :: (a, b) -> {
  >> (a + b);
};

>_ 1;

greet('2');

>_ (sum(2, 1));

>_ sum(1, (sum(1, (sum(1, 1)))));

y = (2 + 2 / 2 + 1 * 3 - 1);

greet(y);
