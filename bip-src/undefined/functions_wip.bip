
--- WIP ---

--- polymorphism? or just type aliases ---
---
type number :: int | float
type string :: array<char>
---

okBuddy ::
    string first,
    string second,
    string third
    -> string {
  return(first + second + third)
}

okBuddy2 ::
    first  = "Ok",
    second = "Buddy",
    third  = "Cool"
    -> {
  return(first + second + third)
}

okBuddy(third = "Dumb")

dynamicArguments :: int args.. -> {
  for arg in args {
    print(arg)
  }
}
